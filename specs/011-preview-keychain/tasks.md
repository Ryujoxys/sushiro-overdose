# 任务

- [x] 分离应用数据根目录和系统用户目录。
- [x] 钥匙串解析失败时终止安装，并提供准确的页面引导。
- [x] 补充路径隔离、安装边界和错误提示测试。
- [x] 构建、重启预览并只读验证；不代替用户确认信任。

## 验证记录

- `go test -race ./...`、`go vet ./...`、构建及 `git diff --check` 通过。
- 前端与 Worker 的 41 项 Node 测试通过。
- 正常 `HOME` 加独立 `SUSHIRO_DATA_HOME` 的环境中，只读查询能找到实际登录钥匙串。
- 原端口 39871 运行 `dev-ux-preview-certfix`。用户继续认证后，页面和状态 API 显示证书、代理步骤完成，进入 `waiting_capture`。
- 未代用户操作系统授权，也未执行取号、预约或取消。凭证捕获和最终取号尚未完成验收。
