# 实施方案

1. 在 `core` 提供应用数据根目录及启动校验，`proxy` 复用该目录；不改变系统目录解析。
2. `platform` 阻止隔离环境修改自启动，并拆出可注入的 macOS 钥匙串解析和证书安装函数。
3. 引擎与前端增加钥匙串不可用错误分支，保留原有锁定与权限错误。
4. 补充无副作用回归测试，重建并在原端口用正常 `HOME` 重启预览。

隔离数据目录不是系统安全沙箱，用户主动认证仍可能申请系统证书与代理权限；不将预览称为无副作用环境。
