# 验收记录

日期：2026-09-18。发布代码：`c83e519a96450c4f646ce3485e177490b515e43c`。

## 本地验证

- 普通与桌面构建的 Go 竞态测试、静态检查通过；模块校验与格式检查通过。
- 前端回归覆盖浏览器和桌面桥接、历史筛选、导出取消、认证确认及手动取号确认，未提交真实订单。
- macOS 原生窗口可加载历史曲线、切换历史数据、打开系统保存框；重复启动仅唤起现有窗口，退出后清理会话文件。
- macOS 双架构与 Windows x64、ARM64 原生构建通过。

## 发布验证

- [发布流水线](https://github.com/Ryujoxys/sushiro-overdose/actions/runs/35318383331)全部通过，Windows x64 原生 EXE 在 Windows runner 上执行 `version` 成功；发布构建使用 Go 1.25.14。
- [v4.0.1](https://github.com/Ryujoxys/sushiro-overdose/releases/tag/v4.0.1) 包含 8 个分发产物和完整校验表。下载全部附件后，8 项 SHA-256 校验均通过。
- 下载的 DMG 通过镜像校验，应用包含 Intel、Apple 芯片双架构，最低系统标记为 macOS 12.0。
- 使用独立数据目录启动下载的 macOS 发布版，确认真实历史曲线与系统保存框正常，取消导出、退出均成功。
- 发布页只保留 v4.0.1；14 个旧 Release 已清理。清理前后的 141 条远程 tag 引用（含附注 tag 的展开引用）完全一致。

## 未覆盖范围

- Windows 真实桌面交互、WebView2 安装流程、ARM64 设备运行尚未实机验证；x64 的 runner 检查只验证加载器与版本入口。
- 未进行真实微信凭证捕获、证书安装、系统代理修改或实际取号；不将这些流程标记为端到端通过。
- 未在用户真实目录开启后台记录或系统自启动。隔离测试不发送真实系统通知或外部推送。
