# 任务

- [x] T01 收敛首页入口和页面文案，补齐本机分析快捷入口。
- [x] T02 改善选店单选、多选、失败重试、键盘焦点和迟到请求处理。
- [x] T03 增加可编辑取号确认，保持认证草稿和显式提交边界。
- [x] T04 简化本机分析启用流程，突出暂停和实际运行状态。
- [x] T05 优化窄屏布局、触控和无障碍提示。
- [x] T06 补充测试，运行隔离检查，记录验证边界。

验证：40 项 JavaScript 测试（39 项前端隔离测试、1 项 Worker 停用检查）、`go test -race ./...`、`go vet ./...`、`go build ./...`、`git diff --check` 均通过。未启动应用或执行截图、真实认证、取号及系统自启动验收，边界见 `docs/ux-improvements-2026-09-17.md`。
