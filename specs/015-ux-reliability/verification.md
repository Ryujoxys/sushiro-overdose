# 验证记录

日期：2026-09-20。基于 v4.0.1 的体验修复，本次提交代码，不发布安装包。

## 已验证

- `go test -race ./...` 全部通过。
- macOS `desktop,production` 全量 race 测试通过；链接器有现有的 LC_DYSYMTAB 警告，没有失败。
- `go vet ./...`、macOS 原生构建及 Windows amd64 / arm64 原生交叉编译通过。
- `node scripts/test-webui-records.mjs` 与 `node scripts/test-history-export.mjs` 通过。
- 浏览器模拟流程通过：图表店名/城市搜索、键盘与无结果、保存门店不隐式开启采集/自启动、导出范围、只读认证返回、单次取号、带确认对象取消、未知结果不重试、离线旧状态提示。
- 实际离线历史接口检查通过：截止时间、按日筛选、历史开关持久化、不计入个人记录、空导出 409、没有浏览器外部请求。
- 1280、390、320 像素宽度无横向溢出。截图保存在本轮本地测试产物中，未纳入仓库。
- macOS 原生窗口确认搜索城市、方向键及回车选店、统一控件尺寸、设置退出确认及应用退出。随后补下拉空间不足时向上展开，并增加几何状态测试。
- 数据保留覆盖超过旧 100,000 行上限再追加、完整读取与导出；取消覆盖账号/凭证/代次/票据变化、只读查询失败、过期、重放及认证活动并发；自启动/退出使用本机模拟测试，不修改系统注册项。

## 验证边界

- 没有操作真实认证、证书安装、系统代理、取号或取消；Windows 完整界面、登录自启动与手机认证尚未实机验证。
- 原生复查工具有一次自动重启未携带隔离参数，读到了默认数据目录并显示应用内公开记录运行；已立即关闭该实例，未操作账号、代理、自启动或号码。不能保证该短暂启动没有追加公开快照，未删除或回滚用户记录。原有预览及独立采集进程保持运行。后续测试包使用独立标识和明确环境参数。
- 原始记录长期增长的分月存储、完整备份导入恢复、打开数据文件夹和完整读屏验收另行处理。
- 旧版后台不支持会话令牌时不会被强杀，需要先使用旧版 `collect stop` 退出。
- 官方取消仍无法原子绑定票据，本地核对不能消除小程序端在核对完成后的并发换号。
