# 任务

- [x] 确认线上数据结构、覆盖范围和只读访问。
- [x] 导出固定历史包与质量摘要。
- [x] 实现离线读取、来源区分和本机覆盖合并。
- [x] 实现可持久化开关与日期筛选。
- [x] 完成单元、浏览器、离线和跨平台检查。
- [x] 更新数据协议、开发说明和隔离预览。
- [x] 补充历史叫号及独立样本统计，支持曲线选时段、键盘和手机拖动查看。

## 验证结果

- 实包 138 家店、238,786 条半小时代表值、81 个记录日，其中有效叫号 118,027 条，截止 2026-09-06 21:30:17 UTC+8，gzip 1,295,261 字节；清单 SHA-256、缺失/零值/非营业计数均与包一致。
- `go test -race ./...`、`go vet ./...`、Node 导出与界面测试通过；macOS、Windows、Linux 的 amd64/arm64 编译通过，未做 Windows 实机验证。
- 模拟浏览器验证记录、手动认证/确认取号、取消和未知结果；真实隔离预览验证首次曲线、具体日期、近期空数据、历史关闭持久化和重新开启、导出不含历史，覆盖 1280/390/320 宽度。
- 叫号与等待分别统计样本天数；验证缺失/零叫号排除、仅叫号有效的日期保留、本机最新代表值覆盖历史、关闭历史同时排除两种曲线、具体日期不使用“通常”措辞。曲线鼠标、键盘和手机拖动均通过浏览器检查。
- 实际页面写设置发现前端 CSRF 请求头与服务器不一致，已统一为 `X-Sushiro-CSRF`，增加服务器中间件与前端一致性测试；没有放宽安全校验。
- 修复自动刷新或调整宽度导致样本表折叠的问题。历史包不会写进个人模型或 JSONL，原有云端接口仍为 410。
- 预览数据目录在 `/tmp/sushiro-history-preview.MSD7Mi/data`。没有发版，没有真实认证、安装证书或下单；浏览器检查不启动真实采集。
