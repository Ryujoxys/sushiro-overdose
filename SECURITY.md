# 安全说明

v4.0 · lite正式版把「公开排队记录」和「本人账号取号」分开。只记录数据不需要账号；手动取号才需要授权使用本人的寿司郎凭证。

## 记录数据时

记录服务访问寿司郎公开接口，保存门店状态、等待时间和叫号快照，不读取个人凭证，不安装证书，不修改代理，也不创建或取消号码。

记录与模型留在本机，不上传共享数据库。内置历史是随程序分发的固定数据包，标注截止时间，不在运行时连接数据库。导出只包含自己的公开记录，不含凭证和个人号码。

没有 GitHub 登录、用户行为埋点或遥测。应用不是完全离线软件：实时查询和采集需要访问寿司郎接口，更新检查需要访问 GitHub。若你自行配置了通知渠道，通知内容会发送到对应服务。

## 手动取号时

取号前确认门店、人数与桌型，仅提交一次；取消号码也需要确认。认证完成不等于确认取号。响应不明确时不会自动重试，请查询当前号码或在官方小程序核对。

凭证保存在本机，用于向寿司郎官方接口认证。请勿把 `config.json`、抓包文件、完整请求头或凭证截图发到公开 Issue。旧云端登录与数据库入口已移除；删除本机配置不等于撤销过去发出的服务端凭证。

## 证书与代理

主动连接微信账号时，程序可能需要生成并安装本机 CA 证书，通过临时代理捕获本人的寿司郎请求。代理只对允许的寿司郎 API 域名解密，其他 HTTPS 连接透传，不读取其加密内容。

CA 私钥保存在 `~/.sushiro-proxy/ca.key`，不上传。凭证和私钥文件请求使用 `0600` 权限；Windows 的访问控制仍取决于账户和目录 ACL，不应把该权限数字当成 Windows 上的完整隔离保证。

电脑认证完成或停止后会恢复系统代理；异常退出后可能仍需在设置中使用「恢复系统代理」。手机 Wi-Fi 代理要在手机端自行关闭。仅删除应用或数据目录，不会自动撤销已安装的证书、系统代理或自启动项。

证书信任会改变设备的信任配置。只记录数据无需安装；不再需要时，可使用 `sushiro uninstall` 按确认步骤清理，并核对手机和电脑上的证书、代理与自启动设置。

## 文件位置

桌面窗口只加载程序内置页面。原生桥仅转发白名单内的本机接口，不访问任意网址；本地 HTTP 服务继续限制回环地址、Host、Origin 和 CSRF。Windows 使用系统 WebView2，缺少运行时时仅引导到微软官网下载，不自动运行远程安装脚本。

窗口唤起令牌短期保存在 `desktop_session.json`，退出时删除，不是寿司郎账号凭证。第三方组件及许可可在设置中查看。

| 位置 | 内容 |
|------|------|
| `~/.sushiro/` | 本机记录、模型、配置、日志和取号凭证 |
| `~/.sushiro-proxy/` | 本机 CA 证书与私钥 |
| `%USERPROFILE%\.sushiro\` | Windows 的应用数据目录 |

预览可用 `SUSHIRO_DATA_HOME` 隔离文件，但它不是系统沙箱。请不要覆盖 `HOME` 或 `USERPROFILE`，这可能破坏钥匙串等系统功能。

## 下载与排障

只从[官方仓库发布页](https://github.com/Ryujoxys/sushiro-overdose/releases)下载，附件的 `checksums.txt` 可用于核对 SHA-256。签名或公证是否可用取决于该次发布配置；系统的未签名或信誉提示既不是恶意软件结论，也不是安全保证。

提交问题时优先使用设置中的脱敏诊断包，并在发送前自行检查。请勿上传个人数据目录、原始凭证或 CA 私钥。

[报告问题](https://github.com/Ryujoxys/sushiro-overdose/issues)时先描述现象、系统与应用版本；涉及可利用漏洞或秘密信息时，先联系维护者确认私密沟通方式，不要直接公开秘密。
